{
  "mounts": [
  "source=${localEnv:HOME}/.ssh,target=/home/vscode/.ssh,type=bind"
],
  "customizations": {
    "vscode": {
      "settings": {
        "dev.containers.defaultExtensions": [],
        "dev.containers.openInContainer": true
      }
    }
  },
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "name": "CPP Dev",
  "postCreateCommand": "apt update && apt install -y gcc-12 g++-12 && update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-12 100 && update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-12 100"
}